# AURA dependencies. Install with:  pip install -r requirements.txt
#
# openai            -> the OpenAI-compatible client. Used by the Nemotron brain
#                      (NVIDIA's endpoint) and any future OpenAI-compatible
#                      provider (e.g. DeepSeek).
# anthropic         -> the official client for talking to Claude (our cloud brain)
# pydantic-settings -> loads & validates configuration/secrets from the .env file
# fastapi           -> the web framework for AURA's local web GUI
# uvicorn           -> the ASGI server that runs the FastAPI app

# Voice providers (local STT / local TTS) are OPTIONAL extras and are NOT
# listed here, to keep this required set lean and the offline test suite
# independent of audio engines. See requirements-voice.txt if you want to
# enable local voice.
openai>=1.40.0
anthropic>=0.39.0
pydantic-settings>=2.6.0
fastapi>=0.115.0
uvicorn>=0.30.0
